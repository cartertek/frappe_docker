name: Cartertek / Build Frappe Image

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      image_tag:
        description: Primary image tag. develop also publishes latest; release tags also publish major aliases.
        required: false
        default: develop
      frappe_ref:
        description: Frappe ref used for layered base/build images and Frappe source.
        required: false
        default: develop
      push:
        description: Publish image to GHCR.
        required: false
        type: boolean
        default: true

permissions:
  contents: read
  packages: write

concurrency:
  group: cartertek-frappe-image-${{ github.ref }}
  cancel-in-progress: true

env:
  IMAGE_NAME: ghcr.io/cartertek/frappe
  FRAPPE_REF: "${{ github.event.inputs.frappe_ref || 'develop' }}"
  PRIMARY_TAG: "${{ github.event.inputs.image_tag || 'develop' }}"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Check image build inputs
        id: changes
        uses: dorny/paths-filter@v3
        with:
          filters: |
            image:
              - apps.json
              - images/layered/**
              - .github/workflows/cartertek-build-image.yml

      - name: Skip image build
        if: ${{ github.event_name != 'workflow_dispatch' && steps.changes.outputs.image != 'true' }}
        run: echo "No image build inputs changed; reporting successful skipped check."

      - name: Setup QEMU
        if: ${{ github.event_name == 'workflow_dispatch' || steps.changes.outputs.image == 'true' }}
        uses: docker/setup-qemu-action@v3
        with:
          image: tonistiigi/binfmt:latest
          platforms: all

      - name: Setup Buildx
        if: ${{ github.event_name == 'workflow_dispatch' || steps.changes.outputs.image == 'true' }}
        uses: docker/setup-buildx-action@v3

      - name: Resolve image tags
        if: ${{ github.event_name == 'workflow_dispatch' || steps.changes.outputs.image == 'true' }}
        id: tags
        shell: bash
        run: |
          set -euo pipefail
          primary="${PRIMARY_TAG}"
          tags=("${IMAGE_NAME}:${primary}")
          if [[ "$primary" == "develop" ]]; then
            tags+=("${IMAGE_NAME}:latest")
          elif [[ "$primary" =~ ^v([0-9]+)\. ]]; then
            major="${BASH_REMATCH[1]}"
            tags+=("${IMAGE_NAME}:v${major}" "${IMAGE_NAME}:version-${major}")
          fi
          {
            echo "tags<<EOF"
            printf "%s\n" "${tags[@]}"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Build smoke-test image
        if: ${{ github.event_name == 'workflow_dispatch' || steps.changes.outputs.image == 'true' }}
        uses: docker/build-push-action@v7
        with:
          context: .
          file: images/layered/Containerfile
          build-args: |
            FRAPPE_IMAGE_PREFIX=frappe
            FRAPPE_PATH=https://github.com/frappe/frappe
            FRAPPE_BRANCH=${{ env.FRAPPE_REF }}
          load: true
          platforms: linux/amd64
          secret-files: |
            apps_json=apps.json
          tags: local/cartertek-frappe:${{ github.run_id }}-${{ github.run_attempt }}

      - name: Smoke test image contents
        if: ${{ github.event_name == 'workflow_dispatch' || steps.changes.outputs.image == 'true' }}
        run: |
          docker run --rm --entrypoint bash local/cartertek-frappe:${{ github.run_id }}-${{ github.run_attempt }} -lc "ls -1 /home/frappe/frappe-bench/apps && test -d /home/frappe/frappe-bench/apps/frappe && test -d /home/frappe/frappe-bench/apps/erpnext && test -d /home/frappe/frappe-bench/apps/hrms && test -d /home/frappe/frappe-bench/apps/sales_engagement_intelligence && cd /home/frappe/frappe-bench && bench version"

      - name: Login to GHCR
        if: ${{ (github.event_name == 'push' && steps.changes.outputs.image == 'true') || (github.event_name == 'workflow_dispatch' && inputs.push) }}
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Build and publish image
        if: ${{ (github.event_name == 'push' && steps.changes.outputs.image == 'true') || (github.event_name == 'workflow_dispatch' && inputs.push) }}
        uses: docker/build-push-action@v7
        with:
          context: .
          file: images/layered/Containerfile
          build-args: |
            FRAPPE_IMAGE_PREFIX=frappe
            FRAPPE_PATH=https://github.com/frappe/frappe
            FRAPPE_BRANCH=${{ env.FRAPPE_REF }}
          platforms: linux/amd64
          push: true
          secret-files: |
            apps_json=apps.json
          tags: ${{ steps.tags.outputs.tags }}
